# Build outputs
**/bin/
**/obj/
**/out/
**/publish/

# IDE / tooling
**/.vs/
**/.vscode/
**/.idea/
**/*.user
.claude/

# Git / CI
.git/
.gitignore
.github/

# Docker / compose (not needed inside the image)
Dockerfile
.dockerignore
docker-compose*.yml

# Docs
**/*.md

# Local-only settings (never ship dev connection strings)
**/appsettings.Development.json
